#ifndef _CONFIG_H_
#define _CONFIG_H_

/**
 * @def MY_SORT
 * @brief Using built-in or written sort
 * @note If this definition is used, the written sort is used, otherwise the built-in sort is used
*/
#define MY_SORT 

/**
 * @def ERR_REPORTS
 * @brief Use logs reports
 * @note We use writing to the log file, otherwise the recording does not occur
*/
#define ERR_REPORTS

#endif